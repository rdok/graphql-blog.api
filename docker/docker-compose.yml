version: "3.7"

services:

  api:
    restart: always
    environment:
      - POSTGRES_PASSWORD
      - NODE_ENV
      - VIRTUAL_PORT
      - PRISMA_CLOUD_SESSION_KEY
      - PRISMA_ENDPOINT
      - JWT_AUTH_SECRET

