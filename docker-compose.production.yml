version: "3.7"

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - VIRTUAL_HOST
      - LETSENCRYPT_HOST
      - LETSENCRYPT_EMAIL

networks:
  default:
    external:
      name: nginx-proxy