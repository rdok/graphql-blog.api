const message = '==========alive================='
const name = 'Cyberpunk 2077'
const location = 'Moon'

const greetings = (name) => {
    return `First man on the moon, ${name}`
}

export { greetings, message, name, location as default }